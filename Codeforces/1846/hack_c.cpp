#include <bits/stdc++.h>
using namespace std;

using ll = long long;
using str = string;

#define sz(x) int((x).size())

int main() {
    cout << 1 << endl;
    cout << 2 << " " << (ll)1e5 << " " << (ll)1e6 << endl;
    for (int j = 0; j < 1e5 - 1; j++) {
        cout << 10 << " ";
    }
    cout << 10 << endl;

    for (int j = 0; j < 1e5 - 1; j++) {
        cout << 5 << " ";
    }
    cout << 5 << endl;

    return 0;
}
